use_frameworks!

target 'InfinityEngine_Example', :exclusive => true do
  pod 'InfinityEngine', :path => '../'
end

target 'InfinityEngine_Tests', :exclusive => true do
  pod 'InfinityEngine', :path => '../'

  pod 'Quick', '~> 0.8'
  pod 'Nimble', '~> 3.0'
  pod 'FBSnapshotTestCase'
  pod 'Nimble-Snapshots'
end
